# Restina Framework

A lightweight PHP framework built on top of Slim framework and Eloquent ORM for rapid API development.

## Installation

You can install the package via composer:

```bash
composer require ivupcn/restina